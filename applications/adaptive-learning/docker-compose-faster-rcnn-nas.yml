version: '2.3'
services:
  teacher:
    build:
      context: ./teachers/
      dockerfile: faster-rcnn-nas-teacher.Dockerfile
    stdin_open: true
    tty: true
    container_name: teacher-container
    volumes:
      - ../../:/repo

    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=0
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - ADAPTIVE_LEARNING_CONFIG=/repo/applications/adaptive-learning/configs/faster_rcnn_nas.ini

  student:
    build:
      context: ./students/
      dockerfile: ssd-mobilenet-v2-student.Dockerfile
    stdin_open: true
    tty: true
    container_name: student-container
    volumes:
    - ../../:/repo
    ports:
      - "2029:6006"
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=0
      - PYTHONPATH=$PYTHONPATH:/models/research:/models/research/slim
      - TF_FORCE_GPU_ALLOW_GROWTH=true
      - ADAPTIVE_LEARNING_CONFIG=/repo/applications/adaptive-learning/configs/faster_rcnn_nas.ini
